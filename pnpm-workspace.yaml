packages:
  - frontend

# Only build scripts we actually depend on may run during install. `@scarf/scarf`
# is analytics telemetry and stays off.
allowBuilds:
  '@scarf/scarf': false
  esbuild: true
  sharp: true
  unrs-resolver: true
