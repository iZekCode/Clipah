{
  "name": "clipah",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter clipah-frontend dev",
    "build": "pnpm --filter clipah-frontend build",
    "start": "pnpm --filter clipah-frontend start",
    "lint": "pnpm --filter clipah-frontend lint",
    "typecheck": "pnpm --filter clipah-frontend typecheck",
    "test": "pnpm --filter clipah-frontend test",
    "generate:api": "pnpm --filter clipah-frontend generate:api",
    "generate:composition": "pnpm --filter clipah-frontend generate:composition",
    "generate:contracts": "scripts/export-openapi.sh && scripts/export-composition-schema.sh && pnpm generate:api && pnpm generate:composition",
    "check:contracts": "scripts/check-contracts-clean.sh"
  },
  "packageManager": "pnpm@11.25.0"
}
