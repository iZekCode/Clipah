import { AssetBrowser } from '@/features/assets/AssetBrowser'

/** Assets: uploaded, stock, generated, and exported media with their provenance. */
export default function AssetsPage() {
  return <AssetBrowser />
}
